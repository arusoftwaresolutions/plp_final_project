{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port $PORT",
    "postDeploy": {
      "command": "python -c \"from app.init_db import init_database; init_database()\""
    }
  }
  
}