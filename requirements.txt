# Ensure latest packaging tools (prevents source builds)
pip>=25.2
setuptools>=75.1
wheel>=0.44

# Core dependencies
fastapi==0.110.0  # Pinned to specific version with pre-built wheels
uvicorn[standard]==0.27.1  # Pinned to specific version with pre-built wheels
gunicorn==21.2.0
python-dotenv==1.0.0
pydantic==2.7.1  # Pinned to specific version with pre-built wheels
pydantic-settings==2.2.1  # Pinned to specific version with pre-built wheels
python-multipart==0.0.9  # Pinned to specific version with pre-built wheels

# Database
sqlalchemy[asyncio]==2.0.29  # Pinned to specific version with pre-built wheels
alembic==1.13.1  # Pinned to specific version with pre-built wheels
psycopg-binary==3.2.3  # Using pre-built binary instead of source build

# Authentication and Security
python-jose[cryptography]==3.3.0  # Pinned to specific version with pre-built wheels
passlib[bcrypt]==1.7.4  # Pinned to specific version with pre-built wheels

# Add any other dependencies below with specific versions
# to ensure consistent builds and avoid source compilation
bcrypt>=4.0.1

# Utilities
email-validator>=2.0.0
python-dateutil>=2.8.2

# Development
pytest>=8.0.0
pytest-asyncio>=0.23.5
httpx>=0.26.0

# Logging
structlog==24.1.0

# Environment
python-dotenv==1.0.0

# Required for production
anyio==4.2.0
click>=8.1.7
h11==0.14.0
idna==3.6
sniffio==1.3.0
typing-extensions==4.9.0
